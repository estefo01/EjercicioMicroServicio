README.

Para realizar esta prueba,  se debe instalar el programa del POSTMAN u otro programa que sirva de cliente para consumir el webservice desarrollador. Luego de haber instalado este aplicativo se debe hacer lo siguiente:

1) Para crear un usuario se debe consumir el siguiente url:
 donde se debe enviar en formato json los datos del usuario a crear, si estuvo correcto los datos, el webservice retornara un mensaje indicando que el Usuario se registro correctamente, en caso contrario retornara error.

